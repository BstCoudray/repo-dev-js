pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                //git url: 'https://github.com/bstcoudray/repo-dev-js', branch: 'main'
                checkout scm
            }
        }
    }
}
